# 実装進捗ステータス

> エージェントはタスクを完了したら該当行の `[ ]` を `[x]` に更新すること。
> 各Phaseの **✅ 完了確認** をすべてパスしてから次のPhaseに進むこと。
> 1回の作業依頼で実装するのは **1タスクのみ**。

---

## Phase 0 ― Next.js 起動確認

**ゴール: `pnpm dev` でブラウザにトップページが表示される**

### セットアップ
- [x] Next.js + TypeScript のプロジェクトを作成する（`pnpm create next-app`・App Router使用）
- [x] `next.config.ts` に `output: 'export'` / `trailingSlash: true` / `images: { unoptimized: true }` を設定する
- [x] Tailwind CSS の設定を確認する（create next-app で同時導入）
- [x] `.env.example` を作成する（`NEXT_PUBLIC_` プレフィックス形式で）
- [x] `.env.local` が `.gitignore` に含まれていることを確認する

### ✅ 完了確認
- [x] `pnpm lint` がエラーなしで完了する
- [x] `pnpm build` が成功し `out/` ディレクトリが生成される
- [x] `pnpm dev` でブラウザに Next.js のトップページが表示される

---

## Phase 1 ― Firebase接続・Googleログイン

**ゴール: Googleサインインが成功し、uidが画面に表示される**

### Firebase SDK 導入
- [ ] Firebase SDK を導入する（`pnpm add firebase`）
- [ ] `src/lib/firebase.ts` でFirebaseを初期化する（`.env.local` の環境変数を使用）

### 認証
- [ ] `useAuth.ts` フックを実装する（サインイン・サインアウト・認証状態監視）
- [ ] `src/app/login/page.tsx` を実装する（Googleサインインボタン）
- [ ] `AuthGuard.tsx`（Client Component）を実装する（`usePathname()` で `/login` を除外してリダイレクト）
- [ ] `src/app/layout.tsx` から `AuthGuard.tsx` を呼び出す
- [ ] サインイン後のトップページにログイン中のユーザー名・uidを表示する

### Firestore初期設定
- [ ] `firestore.rules` にセキュリティルールを記述する（`docs/requirements.md` 参照）
- [ ] Firestoreコンソールでセキュリティルールが反映されていることを確認する

### ✅ 完了確認
- [ ] `pnpm build` が成功する
- [ ] Googleサインインが成功する
- [ ] サインイン後にユーザー名・uidが画面に表示される
- [ ] 未サインイン状態でトップページにアクセスすると `/login` へリダイレクトされる
- [ ] 別のユーザーIDのFirestoreパスにアクセスできないことを確認する

---

## Phase 2 ― ボードの基本CRUD

**ゴール: ボードの作成・切り替え・削除がFirestoreに反映される**

### 型定義・データ層
- [ ] `src/types/index.ts` に `Board` / `Note` / `Link` の型を定義する
- [ ] `src/lib/firestore.ts` にボードの読み書き関数を実装する
- [ ] `useBoards.ts` フックを実装する（一覧取得・新規作成・名前変更・削除・切り替え）

### UI（仮設置・最小限）
- [ ] `Header.tsx` にボード切り替えドロップダウンを実装する
- [ ] サインイン直後、ボードが1件も無い場合は自動で「メインボード」を1件作成する

### ✅ 完了確認
- [ ] `pnpm build` が成功する
- [ ] ボードを新規作成するとFirestoreコンソールに追加される
- [ ] ヘッダーのドロップダウンでボードを切り替えられる
- [ ] ボードを削除すると `isActive: false` に更新される（物理削除されない）
- [ ] 最後の1枚のボードは削除できないことを確認する

---

## Phase 3 ― メモの基本CRUD（配置のみ）

**ゴール: メモを追加・編集・削除でき、Firestoreに反映される（ドラッグ・つなぐ機能はまだ無し）**

### データ層
- [ ] `src/lib/firestore.ts` にメモの読み書き関数を実装する
- [ ] `useNotes.ts` フックを実装する（リアルタイム取得・追加・編集・削除）

### UI（最小限）
- [ ] `NoteInput.tsx` を実装する（「思いついたことをメモする...」入力欄＋＋ボタン）
- [ ] `NoteCard.tsx` を実装する（テキスト表示のみ、固定位置に表示）
- [ ] `Canvas.tsx` を実装する（notesを配列で受け取り、NoteCardを並べるだけ）
- [ ] `src/app/page.tsx` を実装する

### ✅ 完了確認
- [ ] `pnpm build` が成功する
- [ ] 入力欄からメモを追加するとFirestoreコンソールに追加される
- [ ] メモを長押しすると編集モードに入り、テキストを変更できる
- [ ] メモを削除すると `isActive: false` に更新される
- [ ] 別タブで同時に開いていると、追加・編集・削除がリアルタイムに反映される

---

## Phase 4 ― キャンバスの中核操作

**ゴール: メモのドラッグ移動・つなぐ・切る・パン・ズームができる**

### 移動・重なり防止
- [ ] `NoteCard.tsx` にドラッグ移動を実装する（ポインターイベントで位置を更新）
- [ ] 重なり防止ロジック（空きスペースを自動で探す）を実装する
- [ ] ドラッグ終了時のみFirestoreに位置（x, y）を書き込む（ドラッグ中は書き込まない）

### つなぐ・切る
- [ ] `useLinks.ts` フックを実装する（リアルタイム取得・追加・削除）
- [ ] `LinkLine.tsx` を実装する（SVGベジェ曲線でつながりを描画）
- [ ] カード縁の丸ハンドルからドラッグして別カードへつなぐ機能を実装する
- [ ] つながりを切る方法①（線をタップ→「✕ 切る」チップ）を実装する
- [ ] `CanvasControls.tsx` を実装する（拡大・縮小・中央寄せ・件数表示）
- [ ] 「切るモード」ボタンを実装し、モード中は他の操作を無効化する
- [ ] つながりを切る方法②（切るモード中、線を横切るジェスチャーで切断）を実装する
- [ ] メモを削除した際、関連するlinksも合わせて`isActive: false`にする処理を実装する

### パン・ズーム
- [ ] 背景ドラッグでパンできるようにする（ローカル状態のみ、Firestoreには保存しない）
- [ ] ピンチズーム／ホイールズームを実装する

### ✅ 完了確認
- [ ] `pnpm build` が成功する
- [ ] メモをドラッグで移動でき、他のメモと重ならない
- [ ] カード同士をドラッグでつなげる
- [ ] 線をタップして「✕ 切る」で切断できる
- [ ] 切るモードに入り、線を横切るジェスチャーで切断できる
- [ ] 切るモード中は他の操作（パン・ドラッグ等）が無効化されている
- [ ] 背景ドラッグでパン、ピンチ/ホイールでズームができる
- [ ] 別タブで同時に開いていると、位置・つながりの変更がリアルタイムに反映される
- [ ] パン位置・ズーム倍率は別タブに反映されない（ローカルのみ）

---

## Phase 5 ― カード展開アニメーション

**ゴール: カードをクリックすると葉っぱ→角丸四角に滑らかに変形する**

- [ ] カードの`clip-path`を葉っぱ型・角丸四角の2種類定義する
- [ ] `transition`に`cubic-bezier(.16,.84,.44,1)` / 0.42秒を設定する
- [ ] クリックで展開・収納を切り替えるロジックを実装する（全カード対応）
- [ ] 展開時のサイズ変化がテキスト量に応じて自然に決まることを確認する（短文はほぼ変化なし、長文は大きく広がる）
- [ ] 展開・収納アニメーション中、`requestAnimationFrame`でLinkLineの端点を追従させる

### ✅ 完了確認
- [ ] `pnpm build` が成功する
- [ ] 短文・長文どちらのカードもクリックで展開・収納できる
- [ ] 展開アニメーション中、つながっている線がカードの動きに追従する
- [ ] アニメーションがカクつかず滑らかに見える

---

## Phase 6 ― リスト画面

**ゴール: つながりがグループ化され、木構造の箇条書きとCSV/JSON書き出しができる**

### データ層
- [ ] `src/utils/graphUtils.ts` にグループ化・木構造変換ロジックを実装する（`isActive: true`のみ対象）

### UI
- [ ] `GroupList.tsx` を実装する（つながりグループ一覧・単独メモグループ）
- [ ] `GroupItem.tsx` を実装する（木構造の箇条書き表示）
- [ ] 「これを根にする」操作を実装する（一時的な表示状態のみ、保存しない）
- [ ] リスト上でのドラッグによる階層変更を実装する（linksデータを書き換える）
- [ ] `src/utils/exportUtils.ts` にCSV/JSON書き出し処理を実装する（インデント形式）
- [ ] `ExportButtons.tsx` を実装する
- [ ] `src/app/list/page.tsx` を実装する

### ✅ 完了確認
- [ ] `pnpm build` が成功する
- [ ] つながっているメモがグループとして表示され、木構造で箇条書きになる
- [ ] 「これを根にする」操作で表示が切り替わり、再訪問するとデフォルトに戻る
- [ ] ドラッグで階層を変更すると、別タブにもリアルタイムに反映される
- [ ] CSV/JSON書き出しがインデント形式で出力される

---

## Phase 7 ― 設定画面

**ゴール: ボード管理・スキン変更・一括削除・サインアウトが設定画面から行える**

### ボード管理UI
- [ ] `BoardManager.tsx` を実装する（ボード一覧表示・新規作成・名前変更）
- [ ] ボード削除機能を実装する（最後の1枚は非活性にする）

### スキン
- [ ] `SkinSelector.tsx` を実装する（葉っぱ／デフォルト／雲の3種類、ボードごとに保存）
- [ ] スキン切り替えに応じて`clip-path`・配色・ロゴの`mask`色が変わることを確認する

### 危険な操作
- [ ] `DangerZone.tsx` を実装する
- [ ] 「アクティブなボードのメモを全て削除」ボタンを実装する（確認ポップアップ表示→実行）
- [ ] サインアウトボタンを実装する（ヘッダーのユーザーアイコンからも同様の処理を呼べるようにする）

### 画面
- [ ] `src/app/settings/page.tsx` を実装する（3セクションを縦に配置）

### ✅ 完了確認
- [ ] `pnpm build` が成功する
- [ ] ボードの新規作成・名前変更・削除が設定画面から行える
- [ ] スキンを変更すると、そのボードを開いたときに反映される（他のボードには影響しない）
- [ ] 「メモを全て削除」が確認ポップアップを経て実行され、対象ボードのnotes・linksがすべて非表示になる
- [ ] 設定画面・ヘッダーどちらからもサインアウトできる

---

## Phase 8 ― Firebase Hostingへのデプロイ

**ゴール: 公開URLでアプリが動作する**

### デプロイ設定
- [ ] Firebase CLI を開発依存として導入する（`pnpm add -D firebase-tools`）
- [ ] `package.json` の `scripts` に以下を追加する
  ```json
  "firebase:login": "firebase login",
  "firebase:init": "firebase init hosting",
  "deploy": "firebase deploy --only hosting"
  ```
- [ ] `pnpm firebase:login` でFirebaseにサインインする
- [ ] `pnpm firebase:init` を実行し `out` ディレクトリを指定する
- [ ] `firebase.json` の `public` が `"out"` になっていることを確認する

### デプロイ実行
- [ ] `pnpm build` でビルドが成功することを確認する
- [ ] `pnpm deploy` でデプロイする

### ✅ 完了確認
- [ ] 公開URLにアクセスしてアプリが表示される
- [ ] 公開URL上でGoogleサインインが成功する
- [ ] 公開URL上でメモの追加・つなぐ・削除がFirestoreに反映される
- [ ] スマホのブラウザからも公開URLにアクセスできる

---

## Phase 9 ― 品質仕上げ

**ゴール: レスポンシブ・オフライン・競合動作を確認し、全機能が安定して動作する**

### レスポンシブ対応
- [ ] スマートフォン（375px）での表示・操作を確認し崩れを修正する
- [ ] タブレット・PC表示を確認し調整する
- [ ] ロゴ・スキンの見え方をヘッダーサイズで最終確認する

### オフライン・同期
- [ ] Firestoreのオフラインキャッシュを有効化する
- [ ] オフライン状態からの復帰時、データが正しく同期されることを確認する

### 品質
- [ ] エラー発生時のUI表示（トースト通知など）を整える
- [ ] Firestoreセキュリティルールの最終確認をする
- [ ] `docs/constraints.md` のNEVERルールに違反する処理が無いか最終確認する

### ✅ 完了確認
- [ ] `pnpm build` が成功する
- [ ] 他ユーザーのデータにアクセスできないことをセキュリティルールで確認する
- [ ] ネットワークを切断した状態でも既存データが表示される
- [ ] `firebase deploy` でデプロイし公開URLで全機能を最終確認する

---

## 全体完了条件

- [ ] Phase 0〜9 のすべての完了確認がパスしている
- [ ] スマホ・PCの両方でログインからメモの作成・つなぐ・リスト表示・書き出しまで一通り動作する
- [ ] 別タブ・別デバイスで同時に開いても、位置・テキスト・削除・つながりがリアルタイムに反映される
